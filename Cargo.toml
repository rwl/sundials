[package]
name = "sundials"
version = "0.1.0"
license = "BSD-3-Clause"
edition = "2021"

[dependencies]
anyhow = "1"
sundials-sys = { path = "../sundials-sys", features = [ "build_libraries", "klu" ] }
spsolve = { version = "0.1", optional = true }

[features]
default = [ "spsolve" ]