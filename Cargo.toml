[package]
name = "sundials"
version = "0.1.0"
license = "BSD-3-Clause"
edition = "2021"

[dependencies]
anyhow = "1"
sundials-sys = { version = "0.5.1", features = ["build_libraries"] }
faer = { version = "0.19", optional = true }

[dev-dependencies]
sundials-sys = { version = "0.5.1", features = ["build_libraries", "klu"] }

[features]
default = ["faer"]